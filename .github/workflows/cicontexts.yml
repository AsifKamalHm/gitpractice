name: "02- understanding of contexts"
on: 
    workflow_dispatch:
    #schedule:
     # - cron: "*/5 * * * *"
jobs:
    even-info-job:
        runs-on: ubuntu-latest
        steps: 
            - name: Print Trigger Event
              run: |
                echo "my trigger event is ===== ${{ github.event_name }} ===="   
                echo "${{ runner.name }}"
                echo "${{ runner.arch }}"
            

    